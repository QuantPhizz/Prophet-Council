# Copy to .env and fill in. NEVER commit .env.
GOOGLE_CLOUD_PROJECT=
GOOGLE_APPLICATION_CREDENTIALS=
XAI_API_KEY=
LIVE_TRADING=false
# FORCE_HUNT=true   # uncomment for manual hunt runs
# POLY_PRIVATE_KEY=   # live trading only — operator sets this manually
